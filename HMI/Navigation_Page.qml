import QtQuick 2.7

Navigation_PageForm {

//        upButton.onClicked: {
//            naviMap.runJavaScript("google.maps.Map(document.getElementById('map_canvas'), ).panBy(0, -10);");
//        }
//        leftButton.onClicked: {
//                    naviMap.runJavaScript("google.maps.Map(document.getElementById('map_canvas'), ).panBy(-10, 0);");
//                }
//        rightButton.onClicked: {
//                naviMap.runJavaScript("google.maps.Map(document.getElementById('map_canvas'), ).panBy(10, 0);");
//            }
//        downButton.onClicked: {
//                naviMap.runJavaScript("google.maps.Map(document.getElementById('map_canvas'), ).panBy(0, 10);");
//            }
}
